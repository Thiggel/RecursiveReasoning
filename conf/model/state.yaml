name: drm

d_model: 256
n_layers: 3
n_heads: 8
d_ff: 1024
dropout: 0.1
rope_theta: 10000.0

causal: true
recurrence: "state"
slow_steps: 1
fast_steps: 1
act_steps: 8

use_puzzle_emb: true
num_puzzle_ids: -1

state_init: "noise"
state_sigma: 0.02
drm_inject: "concat"
